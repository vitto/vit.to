<div class="aside__buttons">
  <a href="{{ relativePath }}" class="button button--back button--paginate">
    <i class="material-icons button__icon">power_settings_new</i>
  </a>
  {% if previous is defined %}
    {% if previous.draft and metadata.enable.drafts or previous.draft is not defined %}
      <a href="{{ relativePath }}{{ previous.path }}" class="button button--back button--paginate">
        <i class="material-icons button__icon">arrow_back</i>
      </a>
    {% endif %}
  {% endif %}
  {% if next is defined %}
    {% if next.draft and metadata.enable.drafts or next.draft is not defined %}
      <a href="{{ relativePath }}{{ next.path }}" class="button button--back button--paginate">
        <i class="material-icons button__icon">arrow_forward</i>
      </a>
    {% endif %}
  {% endif %}
</div>
